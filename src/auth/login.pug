extends ../base

block content
  .container
    .row
      .col-md-6
        .form-block
          h1 Login to your polytrader account.
          form(method='post', action='/auth/login')
            if formErrors
              - var err = formErrors.filter((x) => x.param == 'email')
              if err.length != 0 && err[0].msg
                .alert.alert-danger
                  i.fa.fa-exclamation-triangle(style="padding-right: 1em;")
                  span=err[0].msg
              .form-group
                label.sr-only(for='name') Email
                - var email_val = err[0].value ? err[0].value : "";
                input#email.form-control(type='email', name='email', placeholder='Email', required='', value=email_val)

            else
              .form-group
                label.sr-only(for='name') Email
                input#email.form-control(type='email', name='email', placeholder='Email', required='')

            if formErrors
              - var err = formErrors.filter((x) => x.param == 'password')
              if err.length != 0 && err[0].msg
                .alert.alert-danger
                  i.fa.fa-exclamation-triangle(style="padding-right: 1em;")
                  span=err[0].msg
            .form-group
              label.sr-only(for='password') Password
              input#password.form-control(type='password', name='password', placeholder='Password', required='')
            .form-group
              button.btn.btn-block.btn-default.btn-big(type='submit', value='submit') Login
      .col-md-offset-1.col-md-5.form-content
        h1 Analyse your trading in five minutes.
        p
          | Simply sign up and entered your poloniex API key and you will have access to a wide arrange of analytical tools and graphs to help you understand your trading capabilities.
        p Elaborate features.

